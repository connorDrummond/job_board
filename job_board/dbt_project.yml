name: job_board
version: 1.0.0
config-version: 2

profile: stg

model-paths: ["models"]

models:
  job_board:
    +materialized: table

    silver:
      +schema: silver

    gold:
      +schema: gold